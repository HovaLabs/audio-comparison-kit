import React from 'react';

import { Container } from '../../design-system/components';

export const PuttingItAllTogether = props => (
  <Container id="putting-it-all-together" {...props}>
    <h2>Putting it all together</h2>
  </Container>
);
